

const signInPage = ({params}) => {
    console.log(params);
    return (
        <div>
            <h1 className="text-5xl mb-8 font-bold">SignInPAge</h1>
        </div>
    );
};

export default signInPage;
